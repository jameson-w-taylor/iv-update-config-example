import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'biometric-iv-example',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
